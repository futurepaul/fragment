[package]
name = "fragment-devstack"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Starts and stops the local fragment stack (a celld dev node for cell/) for xtask and the e2e."

[dependencies]
anyhow = "1"
