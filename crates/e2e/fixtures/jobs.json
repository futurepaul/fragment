{
  "operations": {
    "save": {
      "kind": "mutation",
      "input": {
        "type": "object",
        "required": ["texts", "source"],
        "properties": { "texts": { "type": "array", "items": { "type": "string" }, "maxItems": 100 }, "source": { "type": "string" } }
      }
    },
    "items": { "kind": "query" },
    "digest": { "kind": "job", "input": { "type": "object", "required": ["url"], "properties": { "url": { "type": "string" } } } },
    "ingest": { "kind": "job" },
    "set_flag": { "kind": "mutation" },
    "flag": { "kind": "query" },
    "fragile": { "kind": "job" },
    "nap": { "kind": "job" },
    "count_up": { "kind": "job", "input": { "type": "object", "required": ["n"], "properties": { "n": { "type": "integer", "minimum": 1, "maximum": 256 } } } },
    "careful": { "kind": "job" },
    "probe": { "kind": "job" },
    "parent": { "kind": "job" },
    "wrong": { "kind": "job" },
    "misfit": { "kind": "job" },
    "ping": { "kind": "mutation" },
    "raise": { "kind": "mutation" },
    "boom": { "kind": "job" },
    "tick": { "kind": "mutation" },
    "ticks": { "kind": "query" }
  },
  "channels": { "feed": { "read": "viewer" }, "loop": {}, "alarms": {}, "steps": {} },
  "triggers": [
    { "channel": "inbox", "run": "ingest" },
    { "channel": "loop", "run": "ping" },
    { "channel": "alarms", "run": "boom" },
    { "files": "notes/**", "run": "tick" }
  ]
}
