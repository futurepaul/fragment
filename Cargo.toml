[workspace]
resolver = "2"
members = ["cli", "crates/proto", "crates/nip98", "crates/core", "crates/fakes", "crates/devstack", "crates/e2e", "crates/node", "xtask"]
# cell/ builds for wasm32 with worker-build; spikes/ are throwaway crates.
exclude = ["cell", "agent", "spikes"]

[workspace.package]
edition = "2021"
license = "MIT"

[profile.release]
strip = true
